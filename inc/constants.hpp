#pragma once

namespace Elliot {

// Screen Resolution
const float SCREEN_WIDTH = 512.0F;
const float SCREEN_HEIGHT = 448.0F;

// Tile Settings
const float TILE_SIZE = 32.0F;

// Game Settings
const float PLAYER_SPEED = 3.0F;

// Tyra uses 0-128 for alpha (0 = invisible, 128 = full opaque)
const float PLAYER_SUBMERGED_ALPHA = 64.0F; 
const float PLAYER_NORMAL_ALPHA = 128.0F;

}